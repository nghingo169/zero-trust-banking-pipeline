# Configuration templates

- `environment.variable-overrides.example.json` is the non-secret template for
  each environment owner to copy to the gitignored
  `.databricks/bundle/<bundle-target>/variable-overrides.json` file. The
  workspace host is selected by the owner's Databricks CLI profile.
