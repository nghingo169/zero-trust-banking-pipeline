# Configuration templates

- `demo.variable-overrides.example.json` is the non-secret template for a demo
  owner to copy to the gitignored
  `.databricks/bundle/demo/variable-overrides.json` file. The workspace host is
  selected by the owner's Databricks CLI profile, not committed configuration.
