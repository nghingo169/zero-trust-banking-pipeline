resources:
  schemas:
    poc_card_landing_schema:
      name: ${var.poc_landing_schema}
      catalog_name: ${var.catalog}
    poc_card_bronze_schema:
      name: ${var.poc_bronze_schema}
      catalog_name: ${var.catalog}
    poc_card_silver_schema:
      name: ${var.poc_silver_schema}
      catalog_name: ${var.catalog}
    poc_card_quarantine_schema:
      name: ${var.poc_quarantine_schema}
      catalog_name: ${var.catalog}
  volumes:
    poc_card_daily_snapshot_files:
      name: ${var.poc_landing_volume}
      catalog_name: ${var.catalog}
      schema_name: ${var.poc_landing_schema}
      volume_type: MANAGED
