resources:
  schemas:
    dev_customer_bronze_schema:
      name: ${var.dev_customer_bronze_schema}
      catalog_name: ${var.catalog}
    dev_customer_silver_schema:
      name: ${var.dev_customer_silver_schema}
      catalog_name: ${var.catalog}
    dev_customer_quarantine_schema:
      name: ${var.dev_customer_quarantine_schema}
      catalog_name: ${var.catalog}
