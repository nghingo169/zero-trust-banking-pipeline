resources:
  schemas:
    dev_card_bronze_schema:
      name: ${var.dev_card_bronze_schema}
      catalog_name: ${var.catalog}
    dev_card_silver_schema:
      name: ${var.dev_card_silver_schema}
      catalog_name: ${var.catalog}
    dev_card_quarantine_schema:
      name: ${var.dev_card_quarantine_schema}
      catalog_name: ${var.catalog}
