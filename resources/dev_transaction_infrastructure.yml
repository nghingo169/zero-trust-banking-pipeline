resources:
  schemas:
    dev_transaction_bronze_schema:
      name: ${var.dev_transaction_bronze_schema}
      catalog_name: ${var.catalog}
    dev_transaction_silver_schema:
      name: ${var.dev_transaction_silver_schema}
      catalog_name: ${var.catalog}
    dev_transaction_quarantine_schema:
      name: ${var.dev_transaction_quarantine_schema}
      catalog_name: ${var.catalog}
