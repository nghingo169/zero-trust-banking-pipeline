resources:
  schemas:
    dev_fincrime_bronze_schema:
      name: ${var.dev_fincrime_bronze_schema}
      catalog_name: ${var.catalog}
    dev_fincrime_silver_schema:
      name: ${var.dev_fincrime_silver_schema}
      catalog_name: ${var.catalog}
    dev_fincrime_quarantine_schema:
      name: ${var.dev_fincrime_quarantine_schema}
      catalog_name: ${var.catalog}
