# Legacy source

This directory preserves retired pipeline prototypes and notebooks for reference.
Nothing under `legacy/` is deployed by a Databricks Asset Bundle. Active code
lives in `../pipeline/`, `../data_contracts/`, and
`../replay_validation/`.
